// 5. Quais são as duas formas de se adicionar um comentário no código javascript? dê exemplo em código.

// CONSTANTES:
const A = 2, B = 3, C = 4, D = 5

/* CONDICIONAL: */
if((C * C) >= 100)
{
    console.log(C)
}
else
{
    console.log(`
    O quadrado de ${A} é ${A * A}.
    O quadrado de ${B} é ${B * B}.
    O quadrado de ${C} é ${C * C}.
    O quadrado de ${D} é ${D * D}.`)
}
// Eis o programa. (comentário feito com o atalho ctrl + ; )