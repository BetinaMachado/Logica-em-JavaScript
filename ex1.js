// 1. Crie uma lista contendo 6 nomes de estados brasileiros. Atribua essa lista para uma constante e na sequência, execute o método pop nessa lista
// ( `.pop()` ). Por fim, faça o console.log da lista.

const estados = ['RS', 'SC', 'PR', 'SP', 'RJ', 'ES']

estados.pop()

console.log(estados)

// retira o último elemento da lista estados, atribuindo a uma variável, retorna somente ele:

// var estadoExcluido = estados.pop()
// console.log(estadoExcluido)